<Page x:Class="LocalAIAssistant.Views.Pages.AIChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:LocalAIAssistant.Views.Pages"
      Title="AI对话">

    <Page.Resources>
        <local:MessageBackgroundConverter x:Key="MessageBackgroundConverter"/>
        <local:MessageAlignmentConverter x:Key="MessageAlignmentConverter"/>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="AI 对话" FontSize="28" FontWeight="Bold"
                   Foreground="{StaticResource TextBrush}" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="选择模型:" VerticalAlignment="Center" Margin="0,0,10,0"
                       Foreground="{StaticResource TextSecondaryBrush}"/>
            <ComboBox x:Name="ModelComboBox" Width="200" Padding="10,8"
                      Background="{StaticResource SurfaceBrush}"
                      Foreground="{StaticResource TextBrush}"
                      BorderThickness="0"/>
            <Button x:Name="ClearChatButton" Content="清空对话" Style="{StaticResource ModernButton}"
                    Margin="20,0,0,0" Click="ClearChatButton_Click"/>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource ModernCard}" Margin="0,0,0,20">
            <ScrollViewer x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ChatMessagesControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding IsUser, Converter={StaticResource MessageBackgroundConverter}}"
                                    CornerRadius="12" Padding="15" Margin="5"
                                    HorizontalAlignment="{Binding IsUser, Converter={StaticResource MessageAlignmentConverter}}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Role}" FontSize="12"
                                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Content}" FontSize="14"
                                               Foreground="{StaticResource TextBrush}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="MessageInput" Grid.Column="0" Style="{StaticResource ModernTextBox}"
                     Padding="15" FontSize="14" AcceptsReturn="True" Height="50"
                     KeyDown="MessageInput_KeyDown"/>
            <Button x:Name="SendButton" Grid.Column="1" Content="发送"
                    Style="{StaticResource ModernButton}" Margin="15,0,0,0" Padding="30,15"
                    Click="SendButton_Click"/>
        </Grid>
    </Grid>
</Page>
